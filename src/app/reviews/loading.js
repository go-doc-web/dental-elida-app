'use client';

import React from 'react';

const loading = () => {
  return (
    <div
      style={{
        width: '100wh',
        height: '100vh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: '3.2rem',
      }}
    >
      Loading ...
    </div>
  );
};

export default loading;
